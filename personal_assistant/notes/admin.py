"""Admin configuration for the app.

This module registers the models with the Django admin site so they can be
managed through the admin interface.
"""

from django.contrib import admin

# Register your models here.
