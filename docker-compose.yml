services:
  web:
    image: xraibackx/personal_assistant
    ports:
      - "8000:8000"
    env_file:
      - ./.env.docker
    depends_on:
      - postgres

  postgres:
    image: postgres:latest
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.docker

volumes:
  postgres_data: